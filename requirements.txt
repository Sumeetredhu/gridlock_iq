# GridLock IQ - core stack
flask>=3.0          # serves the web dashboards + live CV detection endpoint
pandas>=2.0
numpy>=1.26
pyarrow>=14
scikit-learn>=1.3
lightgbm>=4.0
scipy>=1.11

# geospatial
h3>=4.0
shapely>=2.0
geopandas>=1.0
osmnx>=2.0          # road-network fusion (optional at runtime; heuristic fallback exists)

# spatial statistics (Getis-Ord Gi* hotspots)
libpysal>=4.9
esda>=2.5

# app / viz
streamlit>=1.40
pydeck>=0.9
plotly>=5.20
folium>=0.16

# optional CV extension (not required to run the core demo)
# ultralytics>=8.2   # YOLO vehicle detection; install separately if you want the CV tab
