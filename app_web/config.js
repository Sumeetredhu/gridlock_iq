// GridLock IQ web config.
// Leave blank for local use (the CV tab calls the same server that serves this page).
// If you deploy the dashboards to Vercel and host the YOLO backend elsewhere
// (Hugging Face Space / Render / Railway), paste that backend's URL here, e.g.:
//   window.GLIQ_API = "https://your-cv-backend.hf.space";
window.GLIQ_API = "";
